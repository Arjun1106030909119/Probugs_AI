{
  "name": "probugs-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "ProBugs Monorepo (Frontend + Backend + Extension)",
  "workspaces": [
    "frontend",
    "backend",
    "extension"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "install:all": "npm install",
    "build:all": "npm run build -w frontend && npm run build -w backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
